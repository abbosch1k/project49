#include <iostream>
using namespace std;

int main() {
    string pass="1234", input;
    int tries=3;

    while(tries--) {
        cin >> input;
        if(input==pass) {
            cout<<"Access";
            return 0;
        }
    }
    cout<<"Locked";
}
